<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.quanta.demo0.mapper.NotificationMapper">

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into tb_notification
        (recipient_user_id, actor_user_id, type, content, payload, is_read, create_time, update_time, is_deleted)
        values
            (#{recipientUserId}, #{actorUserId}, #{type}, #{content}, #{payload}, #{isRead}, #{createTime}, #{updateTime}, #{isDeleted})
    </insert>


    <update id="updateIsRead">
        UPDATE tb_notification
        SET is_read = 1, update_time = NOW()
        WHERE id = #{id}
          AND recipient_user_id = #{userId}
          AND is_deleted = 0
    </update>

    <update id="markAllAsReadByUserId">
        UPDATE tb_notification
        SET is_read = 1, update_time = NOW()
        WHERE recipient_user_id = #{userId}
          AND is_read = 0
          AND is_deleted = 0
    </update>
</mapper>
