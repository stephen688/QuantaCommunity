<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.quanta.demo0.mapper.FollowMapper">

        <insert id="insert" parameterType="com.quanta.demo0.entity.Follow" keyProperty="id" useGeneratedKeys="true">
            INSERT IGNORE INTO tb_user_follow (
                user_id,
                follow_user_id,
               create_time,
               update_time,
               is_deleted)
            VALUES (
                #{userId},
                #{followUserId},
                #{createTime},
                #{updateTime},
                #{isDeleted}
            )
        </insert>
    <update id="update">
        UPDATE tb_user_follow
        SET
          <if test="isDeleted != null">
            is_deleted = #{isDeleted},
          </if>
            <if test="updateTime != null">
            update_time = #{updateTime}
            </if>
        WHERE
           user_id = #{userId}
        and follow_user_id = #{followUserId}
       </update>

</mapper>
