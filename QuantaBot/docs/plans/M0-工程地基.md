# M0-工程地基 实施计划

> **For agentic workers**：本计划按 AGENTS.md §6.0 产出，逐步执行（checkbox 勾选跟踪）；每步走 TDD 红绿节拍（可测步骤）；执行中计划有变，先改本文件再继续。
> **上游依据**：`总计划.md`（M0 节，v1.1）、`AGENTS.md`（红线与工作流）、`docs/技术选型.md` §10.1/§10.3（目录与配置）、`docs/PRD-QuantaBot.md`（背景）

**Goal**：从零建立可运行、可测试、配置收口的 QuantaBot 工程骨架与计划落盘机制，CI 首次全绿。

**Architecture**：src-layout Python 包（uv 管理），分层目录按技术选型 §10.1 占位；配置收口于 `infra/settings.py`（唯一读环境变量处）；FastAPI 应用工厂 `create_app()` 暴露 `/health`；GitHub Actions 三件套门禁。

**Tech Stack**：Python 3.12.13（uv 已托管）+ FastAPI + pydantic-settings + httpx（TestClient 依赖）+ pytest + ruff + hatchling。

**Spec**：见上"上游依据"四份文档；本计划不重复其内容，冲突以上游为准。

## 需求结论（grill-me 对齐，2026-09-13）

1. 范围：整个 M0 一份计划（5 粗任务为内部 Task）；计划文件即 `docs/plans/M0-工程地基.md`。
2. 分支统一：`master` 改名 `main`（旧远程 master 保留不删；gh CLI 未装，GitHub 默认分支切换由管理员网页操作）。
3. `demo0/.trae/rules/project_rules.md` 的删除为管理员有意为之 → 保留，随基线提交（chore，提交信息注明）。
4. push 授权：M0 完成时统一授权——push `feat/m0-project-scaffold` 触发 CI，绿后 `--no-ff` 合回 main 并 push。
5. M0 分支：`feat/m0-project-scaffold` 自 main 切出；合并后 feat 分支保留，是否删除由管理员另行决定。
6. 实现口径：Python 锁 `>=3.12,<3.13` + `.python-version`；依赖最小集 fastapi/uvicorn[standard]/pydantic-settings/httpx + dev 组 pytest/ruff；ruff 规则 `E4/E7/E9/F/I/B`、line-length 100；Settings 用 `env_prefix="quantabot_"`；CI 用 `astral-sh/setup-uv`，触发 push+pull_request。

## Global Constraints（每个 Task 隐含遵守）

- 分层依赖方向 `pipeline/memory → crosscutting → infra`，禁止反向 import；唯一例外 `composition.py`（M0 只建包占位，不涉及）。
- 所有 git 命令在 `QuantaCommunity` 根仓库执行，路径带 `QuantaBot/` 前缀（AGENTS.md §6.4）。
- 禁止改动 `../demo0`、`../demo0-admin`、`../demo0-miniprogram`（已确认的 project_rules.md 删除除外）。
- 提交信息 Conventional Commits（feat/fix/docs/refactor/test/chore）。
- 每步完成后：`uv run ruff format src tests` + `uv run ruff check src tests` + `uv run pytest tests/unit -q` 三件套全绿才提交。
- 密钥只进 `.env`（不入库）；`.env.example` 入库且字段带注释。

---

### Task 0：基线提交、分支改名与切分支

**Files**：无新建；提交现有未提交变更（AGENTS.md 移根、技术选型 v1.6、总计划 v1.1、demo0 删除）；修改 `QuantaBot/总计划.md`（M0 状态 → 进行中）。

**Interfaces**：产出干净工作区 + `main` 分支 + `feat/m0-project-scaffold` 分支，后续 Task 全部在该 feat 分支上进行。

- [ ] **Step 0.0：提交本计划文件（docs/plans/ 载体就位）**

```powershell
# cwd = c:\Users\dwc12\Desktop\QuantaCommunity（下同）
git add QuantaBot/docs/plans/M0-工程地基.md
git commit -m "docs: M0 实施计划落盘（grill-me 对齐结论 + 分步 TDD 任务）"
```

- [ ] **Step 0.1：提交文档变更（按主题分 3 刀）**

> **执行偏差注记（2026-09-13）**：AGENTS.md 的 rename 在开工前已被预暂存（`R` 状态），随 Step 0.0 一刀以 100% 相似度带入；原计划第一刀的 `git add QuantaBot/docs/AGENTS.md` 因旧路径已不存在而失败。实际分刀：①计划文件+rename；②AGENTS v0.4 修订+技术选型 v1.6；③总计划；④demo0 清理——内容完整等价，仅分组不同。

```powershell
# cwd = c:\Users\dwc12\Desktop\QuantaCommunity（下同）
git add QuantaBot/AGENTS.md QuantaBot/docs/AGENTS.md QuantaBot/docs/技术选型.md
git commit -m "docs: AGENTS.md 移至 QuantaBot 根（v0.4.2）；技术选型 v1.6 承接目录/评测/部署明细"
git add QuantaBot/总计划.md
git commit -m "docs: 新增总计划 v1.1（Phase 1 六里程碑 + Phase 0 契约轨道）"
git add demo0/.trae/rules/project_rules.md
git commit -m "chore: 移除 demo0/.trae 旧规则文件（管理员确认有意删除，非误删）"
```

预期：三刀提交成功，`git status --short` 输出为空。

- [ ] **Step 0.2：master 改名 main，切 feat 分支**

```powershell
git branch -m master main
git checkout -b feat/m0-project-scaffold
git branch --show-current
```

预期：输出 `feat/m0-project-scaffold`；`git log --oneline -n 3` 可见刚提交的 3 刀。

- [ ] **Step 0.3：总计划状态回写「进行中」并提交**

修改 `QuantaBot/总计划.md` M0 节：`- **状态**：`未开始`` → `进行中`；§7 变更记录追加：

```markdown
| v1.2 | 2026-09-13 | M0 进入「进行中」（feat/m0-project-scaffold 分支开工；master 改名 main）。 |
```

```powershell
git add QuantaBot/总计划.md
git commit -m "docs: 总计划 M0 状态进行中（v1.2）"
```

---

### Task 1：工程骨架与依赖锁定

**Files**：
- Create：`QuantaBot/pyproject.toml`、`QuantaBot/.python-version`、`QuantaBot/.gitignore`、`QuantaBot/src/quanta_bot/__init__.py`、`QuantaBot/src/quanta_bot/pipeline/__init__.py`、`QuantaBot/src/quanta_bot/memory/__init__.py`、`QuantaBot/src/quanta_bot/crosscutting/__init__.py`、`QuantaBot/src/quanta_bot/infra/__init__.py`（`tests/` 目录随 Task 2 测试文件建立）

**Interfaces**：
- Produces：可 import 的 `quanta_bot` 包（`__version__ = "0.1.0"`）；四个分层包；`uv.lock`（Task 2+ 依赖的锁定基础）。

- [ ] **Step 1.1：写 pyproject.toml**

```toml
[project]
name = "quanta-bot"
version = "0.1.0"
description = "QuantaCommunity AI 评论智能体（被动 @ 触发，四表达模式）"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic-settings>=2.6",
    "httpx>=0.27",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/quanta_bot"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B"]

[tool.pytest.ini_options]
testpaths = ["tests/unit"]
```

- [ ] **Step 1.2：写 .python-version 与 .gitignore**

`.python-version` 内容：`3.12`

```gitignore
# 密钥与本地配置（只进 .env，模板见 .env.example；禁止入库）
.env
# Python 运行产物
.venv/
__pycache__/
*.pyc
# 测试与工具缓存
.pytest_cache/
.ruff_cache/
# 构建产物
dist/
build/
*.egg-info/
```

- [ ] **Step 1.3：写包 __init__.py（头部注释=职责+边界，AGENTS.md §4.2）**

`src/quanta_bot/__init__.py`：

```python
"""quanta_bot —— QuantaCommunity AI 评论智能体服务主体。

分层与依赖方向（硬规则，禁止反向 import）：
- pipeline/      核心链路：触发 → 决策 → 上下文 → 生成（业务，常改）
- memory/        记忆层：接口在此，实现走 infra
- crosscutting/  横切纪律：幂等/熔断/频率/预算/开关/审核（全纯逻辑）
- infra/         唯一触碰外部世界的层（settings/mq/vector/kv/audit_db/tracing）
"""

__version__ = "0.1.0"
```

`src/quanta_bot/pipeline/__init__.py`：

```python
"""pipeline —— 核心链路（按数据流排列）。

职责：触发检测 → 低价值过滤与模式决策 → 上下文组装 → 人格生成。
边界：不触碰网络/存储（外部世界一律走 infra 适配器）；不做幂等/熔断（crosscutting 负责）。
"""
```

`src/quanta_bot/memory/__init__.py`：

```python
"""memory —— 记忆层（对话级/用户级/长期级）。

职责：定义记忆读写接口（ports），供 pipeline 依赖。
边界：实现不写在本包（Redis/Qdrant 适配器在 infra）；不直接 import qdrant-client/redis。
"""
```

`src/quanta_bot/crosscutting/__init__.py`：

```python
"""crosscutting —— 横切纪律（全纯逻辑，重点单测）。

职责：幂等去重、熔断三态、频率约束、成本分档、kill switch 轮询、审核规则预检。
边界：不 import infra/pipeline；不建连接（依赖通过参数注入的端口抽象）。
"""
```

`src/quanta_bot/infra/__init__.py`：

```python
"""infra —— 唯一触碰外部世界的层。

职责：settings 配置收口、MQ/主服务/向量库/Redis/SQLite/Langfuse 适配器。
边界：不写业务决策（pipeline 负责）；被 pipeline/memory/crosscutting 依赖，禁止反向依赖。
"""
```

- [ ] **Step 1.4：uv sync 生成锁文件并验证 import**

```powershell
cd QuantaBot
uv sync
uv run python -c "import quanta_bot; print(quanta_bot.__version__)"
cd ..
```

预期：`uv sync` 成功创建 `.venv` 与 `uv.lock`；输出 `0.1.0`。

- [ ] **Step 1.5：三件套 + 提交（此时 tests/ 尚未建立，ruff 只扫 src）**

```powershell
uv run ruff format src
uv run ruff check src
uv run pytest tests/unit -q     # 无测试文件时退出码 4（path not found，目录尚未建立）——实际执行验证；Task 2 起必须全绿
git add QuantaBot/pyproject.toml QuantaBot/.python-version QuantaBot/.gitignore QuantaBot/uv.lock QuantaBot/src
git commit -m "feat: M0 工程骨架（src 分层包 + uv 锁定 + ruff/pytest 配置）"
```

说明：`uv.lock` 首次生成随本刀提交；`uv run` 若因 cwd 不在 QuantaBot 报找不到项目，命令前先 `cd QuantaBot`（后续各步同）。

---

### Task 2：配置收口（settings.py + .env.example）

**Files**：
- Create：`QuantaBot/src/quanta_bot/infra/settings.py`、`QuantaBot/.env.example`、`QuantaBot/tests/unit/infra/test_settings.py`

**Interfaces**：
- Produces：`Settings`（字段 `app_env: Literal["dev","prod"]`、`host: str`、`port: int`、`fake_mode: bool`；`env_prefix="quantabot_"`，读 `.env`）——Task 3 的 `create_app(settings)` 直接消费。

- [ ] **Step 2.1：写失败的测试（RED）**

`tests/unit/infra/test_settings.py`：

```python
"""Settings 配置收口的行为测试：默认值、环境变量覆盖、fake 开关。"""


from quanta_bot.infra.settings import Settings


def test_settings_defaults() -> None:
    """默认值：dev 环境、127.0.0.1:8000、fake_mode 开（M0/M1 全 fake）。"""
    s = Settings()
    assert s.app_env == "dev"
    assert s.host == "127.0.0.1"
    assert s.port == 8000
    assert s.fake_mode is True


def test_settings_env_override(monkeypatch) -> None:
    """QUANTABOT_ 前缀环境变量可覆盖默认值（pydantic-settings 契约）。"""
    monkeypatch.setenv("QUANTABOT_PORT", "9000")
    monkeypatch.setenv("QUANTABOT_FAKE_MODE", "false")
    s = Settings()
    assert s.port == 9000
    assert s.fake_mode is False
```

- [ ] **Step 2.2：跑测试确认因预期原因失败**

```powershell
uv run pytest tests/unit -q
```

预期：FAIL，报 `ModuleNotFoundError: No module named 'quanta_bot.infra.settings'`。

- [ ] **Step 2.3：最小实现（GREEN）**

`src/quanta_bot/infra/settings.py`：

```python
"""infra/settings —— 配置收口（全仓库唯一读取环境变量/.env 的模块）。

职责：定义 Settings 模型与默认值；业务模块只从这里拿配置，禁止直接触碰 os.environ。
边界：不建任何外部连接；不做依赖连通性检测（/health 负责呈现）；字段与 .env.example 一一对应。
"""

from typing import Literal

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """QuantaBot 运行配置（环境变量前缀 QUANTABOT_，支持 .env 文件）。"""

    model_config = SettingsConfigDict(
        env_prefix="quantabot_",
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )

    # 运行环境：dev=本地开发；prod=生产。M0 仅作标签供 /health 与日志展示
    app_env: Literal["dev", "prod"] = "dev"
    # 服务监听地址与端口（端口为 Agent 自有配置，默认 8000，非主服务联调契约）
    host: str = "127.0.0.1"
    port: int = 8000
    # 全 fake 开关：M0/M1 默认开（外部依赖全走内存假实现），M2 起逐项替换真实现
    fake_mode: bool = True
```

- [ ] **Step 2.4：跑测试确认通过**

```powershell
uv run pytest tests/unit -q
```

预期：2 passed。

- [ ] **Step 2.5：写 .env.example**

```bash
# QuantaBot 环境配置模板——复制为 .env 使用（cp .env.example .env）
# 纪律：密钥只进 .env（不入库）；字段与 infra/settings.py 的 Settings 一一对应
# 值留空 = 使用代码默认值

# 运行环境：dev | prod
QUANTABOT_APP_ENV=dev

# 服务监听地址与端口（Agent 自有配置，默认 8000）
QUANTABOT_HOST=127.0.0.1
QUANTABOT_PORT=8000

# 全 fake 开关：true=外部依赖全走内存假实现（M0/M1 默认）；M2 起逐项替换
QUANTABOT_FAKE_MODE=true
```

- [ ] **Step 2.6：三件套 + 提交**

```powershell
uv run ruff format src tests
uv run ruff check src tests
uv run pytest tests/unit -q
git add QuantaBot/src/quanta_bot/infra/settings.py QuantaBot/.env.example QuantaBot/tests
git commit -m "feat: M0 配置收口（Settings + QUANTABOT_ 前缀 + .env.example）"
```

---

### Task 3：服务入口（server.py /health）

**Files**：
- Create：`QuantaBot/src/quanta_bot/server.py`、`QuantaBot/tests/unit/test_server.py`

**Interfaces**：
- Consumes：`Settings`（Task 2）、`quanta_bot.__version__`（Task 1）。
- Produces：`create_app(settings: Settings | None = None) -> FastAPI`；模块级 `app`（uvicorn 入口 `quanta_bot.server:app`）；`GET /health` 返回 `{status, version, app_env, fake_mode, dependencies}`。

- [ ] **Step 3.1：写失败的测试（RED）**

`tests/unit/test_server.py`：

```python
"""server 入口的行为测试：/health 返回状态/版本/配置摘要与依赖占位。"""

from fastapi.testclient import TestClient

from quanta_bot import __version__
from quanta_bot.infra.settings import Settings
from quanta_bot.server import create_app


def test_health_returns_ok_and_summary() -> None:
    """/health 返回 200 + 版本 + 配置摘要 + 依赖连通占位（M2 起接真实连通性）。"""
    settings = Settings(app_env="dev", fake_mode=True)
    client = TestClient(create_app(settings))
    resp = client.get("/health")
    assert resp.status_code == 200
    body = resp.json()
    assert body["status"] == "ok"
    assert body["version"] == __version__
    assert body["app_env"] == "dev"
    assert body["fake_mode"] is True
    assert isinstance(body["dependencies"], dict)
```

- [ ] **Step 3.2：跑测试确认因预期原因失败**

```powershell
uv run pytest tests/unit -q
```

预期：FAIL，报 `ModuleNotFoundError: No module named 'quanta_bot.server'`（或 import error）。

- [ ] **Step 3.3：最小实现（GREEN）**

`src/quanta_bot/server.py`：

```python
"""server —— FastAPI 入口（/health 与管理端点）。

职责：应用工厂 create_app()；/health 供 compose 健康检查与运维探测（M2 起接入真实依赖连通性）。
边界：不写业务链路（pipeline 负责）；不建外部客户端（composition.py 负责，M1+ 引入）。
"""

from fastapi import FastAPI

from quanta_bot import __version__
from quanta_bot.infra.settings import Settings


def create_app(settings: Settings | None = None) -> FastAPI:
    """构建 FastAPI 应用；settings 缺省时读环境配置（uvicorn 入口路径）。"""
    s = settings or Settings()
    app = FastAPI(title="QuantaBot", version=__version__)

    @app.get("/health")
    async def health() -> dict:
        """健康检查：状态 + 版本 + 配置摘要 + 依赖连通占位。"""
        return {
            "status": "ok",
            "version": __version__,
            "app_env": s.app_env,
            "fake_mode": s.fake_mode,
            "dependencies": {
                "mq": "not_configured",
                "main_service": "not_configured",
                "kv": "not_configured",
                "vector": "not_configured",
                "tracing": "not_configured",
            },
        }

    return app


# uvicorn 入口：uv run uvicorn quanta_bot.server:app（默认端口 8000）
app = create_app()
```

- [ ] **Step 3.4：跑测试确认通过**

```powershell
uv run pytest tests/unit -q
```

预期：3 passed（含 Task 2 的 2 个）。

- [ ] **Step 3.5：真实启动验证（总计划 M0 验收第 2 条）**

```powershell
uv run uvicorn quanta_bot.server:app --port 8000
```

以非阻塞方式启动后，另开命令执行：

```powershell
curl.exe http://127.0.0.1:8000/health
```

预期：返回 `{"status":"ok","version":"0.1.0",...}` 的 JSON；随后停止 uvicorn 进程。

- [ ] **Step 3.6：三件套 + 提交**

```powershell
uv run ruff format src tests
uv run ruff check src tests
uv run pytest tests/unit -q
git add QuantaBot/src/quanta_bot/server.py QuantaBot/tests/unit/test_server.py
git commit -m "feat: M0 服务入口（create_app + /health 健康检查）"
```

---

### Task 4：CI（GitHub Actions）

**Files**：
- Create：`.github/workflows/ci.yml`（**仓库根** `.github/workflows/`——GitHub 只识别仓库根的 workflows；QuantaBot 子目录内的 .github 不生效）

**Interfaces**：
- Produces：push/pull_request 触发的 CI：`uv sync --frozen` → ruff check → ruff format --check → pytest。

- [ ] **Step 4.1：写 workflow 文件（仓库根 .github/workflows/ci.yml）**

```yaml
name: QuantaBot CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: QuantaBot
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v10
        with:
          enable-cache: true
          # 显式锁定解释器：设 UV_PYTHON=3.12，后续 uv sync/run 一律用 uv 托管的 3.12
          #（覆盖 .python-version 与 runner 预装版本的不确定性，缺失时 uv 自动下载托管版）
          python-version: "3.12"
      - name: 安装依赖（按锁文件）
        run: uv sync --frozen
      - name: Lint
        run: uv run ruff check src tests
      - name: Format 检查
        run: uv run ruff format --check src tests
      - name: 单测
        run: uv run pytest tests/unit -q
```

说明：`working-directory: QuantaBot` 使全部步骤在子目录执行；`uv sync` 装 uv.lock 精确版本（CI 与本地同锁）；解释器经 `python-version: "3.12"` → `UV_PYTHON` 显式锁定，不依赖 GitHub runner 预装版本与 `requires-python` 的隐式匹配。

- [ ] **Step 4.2：本地等价预演（CI 同款命令序列）**

```powershell
cd QuantaBot
uv sync --frozen
uv run ruff check src tests
uv run ruff format --check src tests
uv run pytest tests/unit -q
cd ..
```

预期：四步全部成功（`--frozen` 确认锁文件与 pyproject 一致；format --check 零 diff）。此步是 CI 绿的最大概率保证；真实 CI 结果在 Task 5 push 后验证。

- [ ] **Step 4.3：提交**

```powershell
git add .github/workflows/ci.yml
git commit -m "ci: QuantaBot 三件套门禁（uv 冻结安装 + ruff + pytest）"
```

---

### Task 5：push 触发 CI、合并 main、总计划回写

**Files**：无新建；修改 `QuantaBot/总计划.md`（勾选 M0 全部 checkbox、状态 → 完成、§7 追加 v1.3）。

**Interfaces**：Consumes：Task 0-4 全部提交（feat/m0-project-scaffold 分支上的 9 刀：计划落盘 1 + 文档基线 3 + 状态回写 1 + Task 1-4 各 1）。

- [ ] **Step 5.1：push main 与 feat 分支（已获管理员授权，Q4）**

```powershell
git push -u origin main
git push -u origin feat/m0-project-scaffold
```

预期：远程出现 `main`（新建，旧 `master` 保留不动）与 `feat/m0-project-scaffold`；GitHub Actions 对 feat 分支 push 触发 QuantaBot CI。

- [ ] **Step 5.2：确认 CI 全绿**

管理员浏览器打开 `https://github.com/stephen688/QuantaCommunity/actions`，确认 `QuantaBot CI` workflow 首次运行全绿（或把 Actions URL 交由管理员查看后口头确认）。**未绿不得进入 Step 5.3**；若红：读日志定位（多为 yaml/版本/路径问题），修复后重新 push。

- [ ] **Step 5.3：--no-ff 合并回 main 并 push**

```powershell
git checkout main
git merge --no-ff feat/m0-project-scaffold -m "feat: M0 工程地基合入（骨架/配置/入口/CI/计划载体）"
git push origin main
```

预期：merge commit 保留里程碑边界；push 触发 main 的 CI 再跑一次绿。

- [ ] **Step 5.4：总计划验收勾选与状态回写**

逐条核对总计划 M0 验收（4 条）均有实际执行证据后：勾选 M0 五个粗任务 checkbox；`- **状态**：`进行中`` → `完成`；§7 追加：

```markdown
| v1.3 | 2026-09-13 | M0 完成（feat/m0-project-scaffold 合入 main；CI 首次全绿；uv/骨架/配置/入口/载体全部验收）。 |
```

```powershell
git add QuantaBot/总计划.md
git commit -m "docs: 总计划 M0 完成（v1.3）"
git push origin main
```

- [ ] **Step 5.5：提示管理员收尾事项**

- GitHub 网页 `Settings → Branches` 把默认分支从 `master` 切为 `main`（gh CLI 未装，无法代办）；旧 `master` 远程分支按共识保留。
- `feat/m0-project-scaffold` 已合并，是否删除由管理员决定（本计划不执行删除）。

---

## M0 验收对照表（总计划 v1.1 的 4 条）

| # | 总计划验收项 | 本计划落点 | 证据 |
|---|---|---|---|
| 1 | `uv sync --frozen` 成功 | Task 1.4 / 4.2 | 命令实际输出 |
| 2 | uvicorn 启动 + `curl /health` 200 JSON | Task 3.5 | curl 返回 JSON |
| 3 | ruff 零违规 + pytest 全绿（≥1 冒烟用例） | Task 2.4 / 3.4 / 4.2 | 3 passed（Task 3 后） |
| 4 | CI 首次全绿 + `.env` 不入库 | Task 4 / 5.2 + `.gitignore` | Actions 绿勾；`git check-ignore QuantaBot/.env` 输出路径 |

额外核对（Step 5.4 执行）：`git check-ignore QuantaBot/.env` 有输出。

---

## 计划外变更处理

- 执行中发现本计划与实际冲突（命令行为、版本可用性等）：先改本文件对应步骤并注明原因，再继续执行（AGENTS.md §6.0.4）。
- 发现别处问题（与 M0 无关）：记入交付摘要，不顺手修。
